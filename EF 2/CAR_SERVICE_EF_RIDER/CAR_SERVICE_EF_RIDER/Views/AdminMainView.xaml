<UserControl x:Class="CAR_SERVICE_EF_RIDER.Views.AdminMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:md ="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CAR_SERVICE_EF_RIDER.Views"
             xmlns:models="clr-namespace:CAR_SERVICE_EF_RIDER.Models"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    
    <!-- Second color : #ADD8E6 -->
    <!-- Main color : #0f111a -->
    
    <Grid Background="#0f111a">
        
        <Grid.ColumnDefinitions>
            
            <ColumnDefinition Width="0.15*"/>
            <ColumnDefinition/>
            
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="DarkCyan">
            
            <Grid>
                
                <Grid.RowDefinitions>
                    
                    <RowDefinition Height="0.1*"/>
                    <RowDefinition/>
                    
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Background="#ADD8E6">
                    
                    <Button Height="75" Background="Transparent" Foreground="White" Content="{Binding Admin.Name, UpdateSourceTrigger=PropertyChanged}" FontSize="26">
                        
                        <Button.Effect>
                            <DropShadowEffect/>
                        </Button.Effect>
                        
                    </Button>
                    
                </Grid>
                
                <Grid Grid.Row="1">
                    
                    <Grid.RowDefinitions>
                        
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>

                    </Grid.RowDefinitions>

                    <Button Grid.Row="0"  Height="100" Width="140" Background="Transparent" Content="Users" Foreground="White" FontSize="20" Command="{Binding UserCommand}">
                        
                        <Button.Effect>
                            <DropShadowEffect/>
                        </Button.Effect>
                        
                    </Button>
                    
                    
                    <Button Grid.Row="1"  Height="100" Width="140" Background="Transparent" Content="Products" Foreground="White" FontSize="20" Command="{Binding ProductCommand}">
                        
                        <Button.Effect>
                            <DropShadowEffect/>
                        </Button.Effect>
                        
                    </Button>
                    
                    <Button Grid.Row="6"  Height="100" Width="140" Background="Transparent" Content="Exit" Foreground="White" FontSize="20" Command="{Binding ExitCommand}">
                        
                        <Button.Effect>
                            <DropShadowEffect/>
                        </Button.Effect>
                        
                    </Button>
                    
                </Grid>
                
                
            </Grid>
            
        </Border>
        
        <ContentControl Grid.Column="1" Content="{Binding CurrentView,UpdateSourceTrigger=PropertyChanged}"/>
        
        
        
    </Grid>
</UserControl>
