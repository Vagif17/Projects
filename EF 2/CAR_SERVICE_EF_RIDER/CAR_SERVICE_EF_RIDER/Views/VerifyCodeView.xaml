<UserControl x:Class="CAR_SERVICE_EF_RIDER.Views.VerifyCodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:md ="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CAR_SERVICE_EF_RIDER.Views"
             xmlns:models="clr-namespace:CAR_SERVICE_EF_RIDER.Models"
             
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid>
        <Border  Name="AnimatedBorder" Background="WhiteSmoke" CornerRadius="50" Margin="170,60,150,30">
             
                
                 <Border.Effect>
                     
                     <DropShadowEffect/>
                     
                 </Border.Effect>
             
             <Grid>
                
                <Grid.RowDefinitions>
                    
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="1" FontStyle="Italic" FontWeight="SemiBold" FontSize="60" HorizontalAlignment="Center" Margin="9,60,0,0" Text="VERIFY CODE "> </TextBlock>
                
                <Image Grid.Row="0" Grid.RowSpan="2" Source="../Images/ApexAuto(Main_Logo).png"></Image>
      
                 <TextBox Name="UsersCodeTextBox" Text="{Binding UsersCode, UpdateSourceTrigger=Default}" Grid.Row="2" Style="{StaticResource MaterialDesignFloatingHintTextBox}"  HorizontalAlignment="Center" Margin="11,0,0,135" Width="300"  FontSize="11">
                   
                     <md:HintAssist.Hint>
                         <TextBlock>✔️VerifyCode</TextBlock>
                     </md:HintAssist.Hint>
                    
                 </TextBox>
                 
                 
                <Button Grid.Row="2" Command="{Binding VerifyCommand}" HorizontalAlignment="Center"  Background="Transparent"  VerticalAlignment="Bottom" Width="80" Content="Verify" Margin="0,0,0,90">
                    
                    <Button.Effect>
                    
                        <DropShadowEffect/>
                    
                    </Button.Effect>
                    
                </Button>
                 
                
                <Grid.Triggers>
                    <EventTrigger RoutedEvent="Grid.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="AnimatedBorder"
                                                 Storyboard.TargetProperty="Opacity"
                                                 From="0" To="1" Duration="0:0:1.500"/>
                                <DoubleAnimation Storyboard.TargetName="AnimatedBorder"
                                                 Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                 From="0.5" To="1" Duration="0:0:1.500"/>
                                <DoubleAnimation Storyboard.TargetName="AnimatedBorder"
                                                 Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                 From="0.5" To="1" Duration="0:0:1.500"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Grid.Triggers>
                
            </Grid>
            
        </Border>
    </Grid>
</UserControl>
