<UserControl x:Class="CAR_SERVICE_EF_RIDER.Views.UserMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:md ="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CAR_SERVICE_EF_RIDER.Views"
             xmlns:models="clr-namespace:CAR_SERVICE_EF_RIDER.Models"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
   
    <!-- Second color : #ADD8E6 -->
    <!-- Main color : #0f111a -->
    
    
    
    <Grid Background="#0f111a">
        
        <Grid.RowDefinitions>
            
            <RowDefinition Height="0.07*"/>
            <RowDefinition/>
            
            
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="DarkCyan">
            
            <Grid >
                
                <Grid.ColumnDefinitions>
                    
                    <ColumnDefinition Width="0,6*"/>
                    <ColumnDefinition/>
                    
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="1" Background="DarkCyan">
                    
                    <Grid>
                        
                        <Button  Content="{Binding ActiveUser.Name ,UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderBrush="Transparent" Foreground="WhiteSmoke" Margin="30,10,30,10" ></Button>
                        
                    </Grid>
                    
                </Border>
                
                <Border Grid.Column="0" Background="Transparent">
                    
                    <Grid>
                        
                        <Button Command="{Binding CreateAdCommand}" Foreground="White" Background="Transparent"   Content="Create An Ad" Margin="170,0,690,0">
                            <Button.Effect>
                                <DropShadowEffect/>
                            </Button.Effect>
                        </Button>
                        
                        <Button Command="{Binding ExitCommand}" Foreground="White" Background="Transparent"   Content="Exit" Margin="0,0,950,0">
                            <Button.Effect>
                                <DropShadowEffect/>
                            </Button.Effect>
                        </Button>
                        
                        
                        <Button  Background="Transparent" Foreground="White" Content="Search" Margin="940,0,0,0" Height="50">
                            <Button.Effect>
                           
                                <DropShadowEffect/>
                            
                            </Button.Effect>
                        </Button>
                        
                    </Grid>
                    
                </Border>
                
            </Grid>
            
        </Border>
        
        <Grid Grid.Row="1">
            <!-- AI Helped -->
            <ListView ItemsSource="{Binding Items }">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <Image Source="{Binding BitmapImage}" Width="100" Height="100" Stretch="Uniform"/>
                            <StackPanel Margin="10,0,0,0">
                                <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Text="{Binding Description}" Foreground="White" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Price,  StringFormat='Price: {0}₼'}" Foreground="White"/>
                                <TextBlock Text="{Binding DateTime, StringFormat='Date: {0:dd.MM.yyyy}'}" Foreground="White"/>
                                <TextBlock Text="{Binding User.Name, StringFormat='By :{0}'}" Foreground="White" />
                            </StackPanel>
                                <Button VerticalAlignment="Center" Foreground="White"   Background="Transparent" HorizontalAlignment="Right" Content="Buy">
                                    <Button.Effect>
                                        <DropShadowEffect/>
                                    </Button.Effect>
                                </Button>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
            
        </Grid>
        
    </Grid>
</UserControl>
